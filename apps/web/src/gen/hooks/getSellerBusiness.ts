/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { GetSellerBusinessQueryResponse } from "../types/GetSellerBusiness.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetSellerBusinessUrl() {
  const res = { method: "GET", url: `/seller-business` as const };
  return res;
}

/**
 * @summary Listar associações por vendedor
 * {@link /seller-business}
 */
export async function getSellerBusiness(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetSellerBusinessQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetSellerBusinessUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
