/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetBusinessIdQueryResponse,
  GetBusinessIdPathParams,
} from "../types/GetBusinessId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetBusinessIdUrl(id: GetBusinessIdPathParams["id"]) {
  const res = { method: "GET", url: `/business/${id}` as const };
  return res;
}

/**
 * @summary Obter empresa por ID
 * {@link /business/:id}
 */
export async function getBusinessId(
  id: GetBusinessIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetBusinessIdQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetBusinessIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
