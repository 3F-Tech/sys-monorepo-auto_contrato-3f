/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  PostContractsIdCancelMutationResponse,
  PostContractsIdCancelPathParams,
} from "../types/PostContractsIdCancel.ts";

function getPostContractsIdCancelUrl(
  id: PostContractsIdCancelPathParams["id"],
) {
  const res = { method: "POST", url: `/contracts/${id}/cancel` as const };
  return res;
}

/**
 * @summary Cancelar um contrato pendente no Clicksign e no sistema
 * {@link /contracts/:id/cancel}
 */
export async function postContractsIdCancel(
  id: PostContractsIdCancelPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostContractsIdCancelMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getPostContractsIdCancelUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
