/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { PostCommercialCostsMutationResponse } from "../types/PostCommercialCosts.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getPostCommercialCostsUrl() {
  const res = { method: "POST", url: `/commercial-costs` as const };
  return res;
}

/**
 * @summary Definir custos comerciais para um mês
 * {@link /commercial-costs}
 */
export async function postCommercialCosts(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostCommercialCostsMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getPostCommercialCostsUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
