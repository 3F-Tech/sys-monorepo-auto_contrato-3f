/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  GetSellerBusinessIdQueryResponse,
  GetSellerBusinessIdPathParams,
} from "../types/GetSellerBusinessId.ts";

function getGetSellerBusinessIdUrl(id: GetSellerBusinessIdPathParams["id"]) {
  const res = { method: "GET", url: `/seller-business/${id}` as const };
  return res;
}

/**
 * @summary Listar associações por empresa
 * {@link /seller-business/:id}
 */
export async function getSellerBusinessId(
  id: GetSellerBusinessIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetSellerBusinessIdQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetSellerBusinessIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
