/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  GetContractsHeadHeadidQueryResponse,
  GetContractsHeadHeadidPathParams,
} from "../types/GetContractsHeadHeadid.ts";

function getGetContractsHeadHeadidUrl(
  headId: GetContractsHeadHeadidPathParams["headId"],
) {
  const res = { method: "GET", url: `/contracts/head/${headId}` as const };
  return res;
}

/**
 * @summary Listar contratos da equipe (por ID do Head)
 * {@link /contracts/head/:headId}
 */
export async function getContractsHeadHeadid(
  headId: GetContractsHeadHeadidPathParams["headId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetContractsHeadHeadidQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetContractsHeadHeadidUrl(headId).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
