/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type { GetContractsQueryResponse } from "../types/GetContracts.ts";

function getGetContractsUrl() {
  const res = { method: "GET", url: `/contracts` as const };
  return res;
}

/**
 * @summary Listar todos os contratos
 * {@link /contracts}
 */
export async function getContracts(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetContractsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetContractsUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
