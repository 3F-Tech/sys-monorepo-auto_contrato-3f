/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type { GetCacQueryResponse } from "../types/GetCac.ts";

function getGetCacUrl() {
  const res = { method: "GET", url: `/cac` as const };
  return res;
}

/**
 * @summary Listar CAC por BU para um mês específico (com fallback histórico)
 * {@link /cac}
 */
export async function getCac(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetCacQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: getGetCacUrl().url.toString(), ...requestConfig });
  return res.data;
}
