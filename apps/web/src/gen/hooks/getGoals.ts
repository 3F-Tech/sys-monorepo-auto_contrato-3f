/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type { GetGoalsQueryResponse } from "../types/GetGoals.ts";

function getGetGoalsUrl() {
  const res = { method: "GET", url: `/goals` as const };
  return res;
}

/**
 * @summary Listar metas filtradas por visibilidade
 * {@link /goals}
 */
export async function getGoals(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetGoalsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: getGetGoalsUrl().url.toString(), ...requestConfig });
  return res.data;
}
