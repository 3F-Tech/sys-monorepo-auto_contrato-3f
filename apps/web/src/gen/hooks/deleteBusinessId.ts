/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  DeleteBusinessIdMutationResponse,
  DeleteBusinessIdPathParams,
} from "../types/DeleteBusinessId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getDeleteBusinessIdUrl(id: DeleteBusinessIdPathParams["id"]) {
  const res = { method: "DELETE", url: `/business/${id}` as const };
  return res;
}

/**
 * @summary Deletar empresa
 * {@link /business/:id}
 */
export async function deleteBusinessId(
  id: DeleteBusinessIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeleteBusinessIdMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getDeleteBusinessIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
